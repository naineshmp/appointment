# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Appointment Lookup' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Appointment Lookup

  target 'Appointment LookupTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Appointment LookupUITests' do
    inherit! :search_paths
    # Pods for testing
  end

pod 'Firebase/Core'
  pod 'Firebase/Auth'
pod 'Firebase/Database'
end
